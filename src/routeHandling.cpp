#include "../include/routeHandler.hpp"       // Incluye el archivo de encabezado de la clase RouteHandler.

RouteHandler::RouteHandler() {}   // Implementación del constructor de la clase RouteHandler.


/*
RouteHandler::RouteHandler() {}: 
    Implementación del constructor de la clase RouteHandler. En este caso, el constructor está vacío y no realiza ninguna operación especial.

void RouteHandler::add_route(const std::string& uri, const std::string& http_method, std::function<std::string()> handler) { ... }: 
    Implementación de la función add_route, que agrega una ruta junto con su método HTTP y función controladora correspondiente al mapa routes.

std::function<std::string()> RouteHandler::get_handler(const std::string& uri, const std::string& http_method) { ... }: 
    Implementación de la función get_handler, que busca y devuelve la función controladora correspondiente según la URI y el método HTTP especificados.
*/

/*
execve, dup, dup2, pipe, strerror, gai_strerror,
errno, dup, dup2, fork, htons, htonl, ntohs, ntohl,
select, poll, epoll (epoll_create, epoll_ctl,
epoll_wait), kqueue (kqueue, kevent), socket,
accept, listen, send, recv, bind, connect,
getaddrinfo, freeaddrinfo, setsockopt, getsockname,
getprotobyname, fcntl, close, read, write, waitpid,
kill, signal, access.
*/